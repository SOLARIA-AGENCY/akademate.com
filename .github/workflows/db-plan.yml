name: DB Plan (placeholder)

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'packages/db/**'
      - 'apps/payload/**'

jobs:
  plan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Placeholder DB plan
        run: echo "TODO: add Drizzle/Payload plan and approval gates"
