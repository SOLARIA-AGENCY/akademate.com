# Prompt · Akademate Prefab SaaS (forget-proof)

You are working on **Akademate** as a **Prefab SaaS**, not as artisan custom software.

## Analogy
- **CEP** = artisan house that already charges (MVP / on-prem proof).
- **Akademate** = prefab plant: certified Motor + complete Kit + Ensamble (vertical × scale S/M/L + paid modules).

## Layers (never skip)
1. **Motor** — core logic/data/billing elevated from charging CEP; **BRIK64-certified** billing surface. Maps to Canonical Core + capabilities.
2. **Kit** — Gate 0 `DESIGN.md` + full design system before assemble. Catalog UI only (repo primitives → shadcn → named Magic/Aceternity slug). Akademate chrome = `@shadcn/sidebar-16`. No invent.
3. **Ensamble** — Blueprint vertical × scale (S/M/L) + optional modules (app, QR, signage, videoconference) **only when paid or GO**.

## Cash-preserving order
A) Certify Motor that charges → B) DESIGN.md + dashboard kit → C) **one S vertical** (default first: Yoga — landing + cobros + asistencias) → D) modules after payer.  
**Forbidden:** ten verticals or full L before first paying S client. **InScouter** = second mold later.

## Architecture honesty
- Product mold: Master Architecture (Core + Blueprint + Plan×Deployment).
- Current prod topology: follow `docs/ARCHITECTURE.md` (do not claim full Cloudflare serverless production unless a linked target doc says so; CF edge is optional per Master).
- CEP HOLD rules stay until Commander lifts them.

## Roles
ECO routes. AUDIT coordinates akademate.com. BUILD AKADEMATE implements. No external send/charge/publish without Commander yes.

## Done means
Named slug/surfaces, DESIGN.md present for visual work, one S closed loop that can charge, no fake tenants/revenue.
